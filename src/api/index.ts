import { jsonInstance, multipartInstance } from "./jsonInstance";
import { getJsonConfig, getMultipartConfig } from "./config";

export {
    jsonInstance,
    multipartInstance,
    getJsonConfig,
    getMultipartConfig,
};